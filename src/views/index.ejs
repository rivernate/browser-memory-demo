<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Memory Demonstration</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa;
            color: #343a40;
        }
        .container {
            margin: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        header {
            background-color: #007bff;
            color: white;
            padding: 20px;
            width: 100%;
            text-align: center;
        }
        .section {
            margin: 20px 0;
            padding: 20px;
            width: 100%;
            max-width: 800px;
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .section h2 {
            margin-bottom: 10px;
            font-size: 24px;
        }
        .list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 10px;
            list-style-type: none;
            padding: 0;
        }
        .list li {
            background-color: #f1f1f1;
            padding: 10px;
            border-radius: 4px;
            text-align: center;
            transition: background-color 0.3s;
        }
        .list li a {
            text-decoration: none;
            color: #007bff;
            font-size: 18px;
        }
        .list li:hover {
            background-color: #e2e2e2;
        }
        footer {
            margin-top: 20px;
            padding: 10px;
            width: 100%;
            text-align: center;
            font-size: 14px;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <header>
        <h1>Memory Demonstration</h1>
    </header>
    <div class="container">
        <div class="section">
            <h2>Memory Areas</h2>
            <ul class="list">
                <% memoryAreas.forEach(area => { %>
                    <li><a href="/<%= area %>"><%= area.replace('-', ' ').toUpperCase() %></a></li>
                <% }) %>
            </ul>
        </div>
        <div class="section">
            <h2>Memory Leaks</h2>
            <ul class="list">
                <% memoryLeakTypes.forEach(type => { %>
                    <li><a href="/memory-leaks/<%= type %>"><%= type.replace('-', ' ').toUpperCase() %></a></li>
                <% }) %>
            </ul>
        </div>
    </div>
    <footer>
        &copy; <%= new Date().getFullYear() %> Memory Demonstration. All rights reserved.
    </footer>
</body>
</html>

