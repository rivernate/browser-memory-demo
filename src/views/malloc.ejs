<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Malloc Memory Example</title>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const largeArray = new Array(1000000).fill(0).map((_, i) => i);
            console.log('Large array created with malloc memory:', largeArray);
        });
    </script>
</head>
<body>
    <h1>Malloc Memory Example</h1>
    <h2>Not Functional!</h2>
    <p>Check the console for details on the large array created with malloc memory.</p>
</body>
</html>

