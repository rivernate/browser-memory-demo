<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Circular References Memory Leak</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .container {
            margin: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .description {
            text-align: left;
            max-width: 600px;
        }
        pre {
            background-color: #f1f1f1;
            padding: 10px;
            border-radius: 4px;
            overflow: auto;
            max-width: 600px;
        }
        .heap-size {
            margin-top: 20px;
            font-size: 18px;
        }
        button {
            margin: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Circular References Memory Leak</h1>
        <div class="description">
            <p><strong>Explanation:</strong> A circular reference leak occurs when two or more objects reference each other, creating a cycle. This prevents garbage collection from freeing up the memory used by these objects.</p>
            <p><strong>Example Code:</strong></p>
            <pre>
function createCircularReference() {
    const objA = {};
    const objB = {};
    objA.b = objB;
    objB.a = objA; // Circular reference
}
            </pre>
        </div>
        <button id="start-button">Start Leak</button>
        <div id="output"></div>
        <div id="heap-size" class="heap-size">Heap Size: N/A</div>
    </div>
    <script src="/js/circular-references.js"></script>
</body>
</html>

